// Common Widgets
import 'package:flutter/material.dart';

/// An empty widget.
const ew = SizedBox(); // emptyWidget

/// An empty widget with width set to infinity.
const ewWide = SizedBox(width: double.infinity); // emptyWidgetWide

// Margins
/// A horizontal margin of 4.0.
const hm4 = SizedBox(width: 4.0); // horizontalMargin4

/// A horizontal margin of 8.0.
const hm8 = SizedBox(width: 8.0); // horizontalMargin8

/// A horizontal margin of 12.0.
const hm12 = SizedBox(width: 12.0); // horizontalMargin12

/// A horizontal margin of 16.0.
const hm16 = SizedBox(width: 16.0); // horizontalMargin16

/// A horizontal margin of 24.0.
const hm24 = SizedBox(width: 24.0); // horizontalMargin24

/// A horizontal margin of 32.0.
const hm32 = SizedBox(width: 32.0); // horizontalMargin32

/// A horizontal margin of 48.0.
const hm48 = SizedBox(width: 48.0); // horizontalMargin48

/// A vertical margin of 4.0.
const vm4 = SizedBox(height: 4.0); // verticalMargin4

/// A vertical margin of 8.0.
const vm8 = SizedBox(height: 8.0); // verticalMargin8

/// A vertical margin of 12.0.
const vm12 = SizedBox(height: 12.0); // verticalMargin12

/// A vertical margin of 16.0.
const vm16 = SizedBox(height: 16.0); // verticalMargin16

/// A vertical margin of 24.0.
const vm24 = SizedBox(height: 24.0); // verticalMargin24

/// A vertical margin of 32.0.
const vm32 = SizedBox(height: 32.0); // verticalMargin32

/// A vertical margin of 48.0.
const vm48 = SizedBox(height: 48.0); // verticalMargin48

// Paddings
/// An empty padding.
const ep = EdgeInsets.zero; // emptyPadding

/// A horizontal padding of 4.0.
const hp4 = EdgeInsets.symmetric(horizontal: 4.0); // horizontalPadding4

/// A horizontal padding of 8.0.
const hp8 = EdgeInsets.symmetric(horizontal: 8.0); // horizontalPadding8

/// A horizontal padding of 12.0.
const hp12 = EdgeInsets.symmetric(horizontal: 12.0); // horizontalPadding12

/// A horizontal padding of 16.0.
const hp16 = EdgeInsets.symmetric(horizontal: 16.0); // horizontalPadding16

/// A horizontal padding of 24.0.
const hp24 = EdgeInsets.symmetric(horizontal: 24.0); // horizontalPadding24

/// A horizontal padding of 32.0.
const hp32 = EdgeInsets.symmetric(horizontal: 32.0); // horizontalPadding32

/// A horizontal padding of 48.0.
const hp48 = EdgeInsets.symmetric(horizontal: 48.0); // horizontalPadding48

/// A vertical padding of 2.0.
const vp2 = EdgeInsets.symmetric(vertical: 2.0); // verticalPadding2

/// A vertical padding of 4.0.
const vp4 = EdgeInsets.symmetric(vertical: 4.0); // verticalPadding4

/// A vertical padding of 8.0.
const vp8 = EdgeInsets.symmetric(vertical: 8.0); // verticalPadding8

/// An all-around padding of 4.0.
const ap4 = EdgeInsets.all(4.0); // allPadding4

/// An all-around padding of 8.0.
const ap8 = EdgeInsets.all(8.0); // allPadding8
